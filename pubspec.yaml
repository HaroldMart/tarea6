name: tarea6
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  cached_network_image: ^3.2.0
  http: ^0.13.3
  html_unescape: ^2.0.0
  flutter_html: ^2.1.1
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/Toolbox.png
    - assets/profile.jpg